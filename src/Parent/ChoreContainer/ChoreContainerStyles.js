import styled from 'styled-components';

const ChoreContainerStyles = styled.div`
  background: #fff;
  border-radius: 0.5rem;
  margin: 0 auto;
  margin-top: 1rem;

  .header-container {
    border-top: none;
    padding: 1.25rem 1rem;
  }
`;

export default ChoreContainerStyles;
